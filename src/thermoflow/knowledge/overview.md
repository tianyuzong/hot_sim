# ThermoFlow 工作流

## workflow

ThermoFlow 将工件几何、材料、边界、网格和求解设置保存为版本化仿真研究。推荐流程是导入 STL、确认长度单位、生成未确认草案、检查参数、确认输入、生成真实网格、求解并查看温度和热流结果。

结构化草案不会直接执行求解。确定性求解器只接受通过策略校验且经过用户确认的研究。

## records

项目、工件、研究、网格、结果、后台任务和结果优化 Agent 记录由服务端保存。浏览器只保存工作区导航状态，不保存权威仿真输入或结果场。
