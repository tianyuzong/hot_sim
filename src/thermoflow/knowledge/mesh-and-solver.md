# 网格与求解

## mesh

STL 研究使用确定性的体素网格。目标单元尺寸越小，通常越能分辨局部特征，但活动单元数、内存和求解时间会增加。网格生成后会报告活动单元、连通性、体积偏差、边界映射和质量状态。

## review

网格 warning 是需要用户检查的非阻断风险；blocked 是不能通过勾选跳过的硬错误。warning 研究必须查看真实网格并明确接受风险后才能求解。

## backend

解析长方体使用 analytic_box_v1 作为稳态导热回归基准；STL 使用 voxel_stl_v1。求解器不执行模型生成的代码或命令。
