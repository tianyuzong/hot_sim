# 边界与热源

## conditions

当前 STL 热模型支持固定温度、局部功率热源、全局对流、区域热流、区域对流、环境辐射和有限的组件热接触。区域条件必须绑定到存在的组件表面或已保存区域。

## units

温度使用 K，长度使用 mm，时间使用 s，功率使用 W，热流密度使用 W/m²。摄氏温度输入必须先转换为绝对温度；用户没有确认的 STL 单位不能被 Agent 猜测。

## mapping

热源和区域边界会映射到体素网格单元。小区域、尖锐边缘、开放表面和较粗网格可能造成映射近似，实际映射数量和位置应以网格报告为准。
