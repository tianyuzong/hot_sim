# Agent 与工作台

## modeling-agent

建模 Agent 可以根据自然语言补齐草案、追问关键参数并提交已确认的后台任务。Agent 建议、系统默认和用户事实必须区分；Agent 不能绕过输入确认、策略校验或网格风险检查。

## result-agent

结果优化 Agent 只从已完成研究开始，在用户授权的变量和轮数范围内创建独立候选研究。它不能替代首次建模，也不能直接修改已完成研究。
