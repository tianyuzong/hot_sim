# ThermoFlow 需求实施记录

基准：[完整需求说明](THERMOFLOW_REQUIREMENTS.md)，2026-09-05。
目标仍是完整工程仿真平台；以下是阶段进度，不是整体验收通过声明。

## 瞬态真实热扩散可视化批次（2026-09-09）

- 新瞬态研究按实际体素间距和材料热扩散率生成最多 201 个非均匀输出时刻；所有输出帧均由后向欧拉真实推进得到，内部步长不超过用户设置的最大时间步，不做插值或复制帧。
- 瞬态结果首次打开默认显示“热扩散”模式，以本帧最低温度为参考值、使用全时段固定空间温差色标；“绝对温度”继续使用全时段固定绝对温标。拖动和播放时改变的是工件颜色，图例的低/中/高数值保持不变。
- 播放数据一次加载到本地；新研究保存 201 帧，60 秒时间轴可连续拖动，不再按帧请求温度场。
- 结果页按整个时段的最低/最高温度检查每个组件的材料有效温区。超温时明确提示常物性外推、温度相关物性和相变未建模，禁止把外推结果直接当作工程结论。
- Firework 新建研究 `study-d7b4e406b880408aaec936e82f45517d`，使用 C110、4 个点热源共 70 W、60 s、最大积分步长 1 s；RTX 4090 的 `cuda-cupy-cg` 求解生成 201 帧，首个正时刻 0.0088028133 s，热点首先出现在 40 W 主热源映射位置。
- 同输入 CPU/CUDA 结果最大温度差低于 `1×10⁻¹¹ K`。Python 全量回归 174 项通过；浏览器/Node 全套通过；Firework 部署后专项 20 项通过。真实页面桌面/移动布局、固定色标、早期与末态颜色差异、材料告警和无控制台错误均已验收。
- 演示网格在薄方向只有 3 层，体素体积偏差 58.4%；页面保留质量警告。结果峰值 1519.15 K 超出 C110 有效温区，因此本研究用于功能验收，不是网格/材料收敛后的工程结论。
- 详见 [瞬态热扩散验收记录](verification/transient-diffusion-20260909.md) 和根目录中文使用说明 `USAGE_SUMMARY.md`。

## 热仿真数值修正批次（2026-09-07）

- 按用户要求暂停服务器 Codex 在线联调，优先修复不依赖模型的热仿真链路。
- 复现并修正多组件材料错配：不再把体素质心排序与原始组件排序直接配对。
  网格、求解、结果视图和单元探针统一使用原始 STL 的稳定组件归属；无法唯一映射则阻止求解。
- 导热恢复 `G = k_harmonic A / d`，全局及区域对流均使用实际外露体素面面积。
  移除把整装配体积比例施加到局部导热/换热的处理，隔离组件不再相互改变数值系数。
- 瞬态热容按各封闭组件自己的 STL 物理体积、密度和比热分别计算；开放组件继续使用近似重建体积。
- 热接触单元的热流矢量采用内部面与接触面的平均，不再重复叠加；方向采用配对表面法向。
- 新增 9 项回归测试，包含 mm/um/m 三种单位、不同材料/体积、独立物体不干扰原温度场、
  三维结果/探针归属和一维接触热流。对齐导热网格用解析线性温度作直接校验，
  不再要求已经达到浮点舍入误差的网格误差继续单调减小。
- 独立代码复核补充了粗网格归属歧义的诊断视图回归：禁止求解，但仍可查看无组件标识的网格；
  原始几何损坏仍拒绝读取。复核后无 Critical/Important 未解决项。
- 首轮专项 46 项通过；复核后的组件/显示专项 15 项通过；最终全套 152 项通过（63.43 秒）。基础错误及导入规范检查通过；
  扩展 Ruff 规则仍报告 5 项原有代码风格告警，没有将其记为全规则通过。
- 求解结果版本更新为 `1.5`；旧输入规格不改版，历史数值文件不覆盖。
  旧研究需复制、重新确认、生成网格并重算后才得到修正结果。
- 原 `127.0.0.1:18080` 服务已重启生效，仍使用 `.e2e-data`、CPU 后端及原规划器配置；
  健康检查与工作台 HTTP 200，原有研究保留，未发起真实模型请求。
- 详见 [数值验证报告](THERMAL_VERIFICATION_20260907.md)。本批不宣称 GPU、真实浏览器或完整热力耦合平台验收通过。

以下是历史批次记录；其中环境限制及测试数量应结合上方最新记录和
[后端配置说明](CODEX_HARNESS_SETUP.md) 阅读。

## Codex Harness 接入批次

- 新增 `THERMOFLOW_PLANNER=codex`，通过部署已有的 Codex CLI 生成结构化建模草案与受授权的优化决策。
  本机忽略版本管理的 `.env` 已指向用户提供的 Codex 目录；不修改该目录或共享虚拟环境，不读取/复制认证文件。
  其他部署仍可显式选用 OpenAI API 或离线规则模式，不做静默回退。
- 沿用当前草案、紧凑几何、材料目录及最近对话上下文；通过 stdin 传递，使用 `--output-schema` 约束最终 JSON，
  服务端继续校验 Schema、材料来源、参数引用与能力。Codex 返回的确认声明不能成为用户确认。
- 单次调用关闭 shell/MCP/插件/hooks/浏览器/多 Agent 功能，保持只读沙箱、不申请扩大权限。
  原始 CLI 输出不进入前端；临时响应、Schema、日志与状态目录清理。请求有超时、进程组回收和单进程全局并发限制。
- 新增结构化响应、上下文、授权阻断、错误脱敏、超时真实子进程回收及配置兼容测试；保留 OpenAI 与离线回归。
- 验证：`tests/test_codex_harness.py` 新增 19 项；全套 123 项通过，耗时 57.44 秒。
  Ruff、JavaScript 语法、Node 建模状态/Codex 标签及 Three.js 几何数值检查通过。
  18019 端口完成 8 秒限时服务启动/关闭检查，但未验证用户可访问性，未留下运行服务。
  Playwright 仍在本地 socket 创建阶段返回 `EPERM`，未执行页面步骤或取得截图。
- 真实探测：本机 Codex 0.153.4 支持非交互 Schema 输出，但在指定目录初始化时返回只读文件系统错误。
  临时日志/状态目录覆盖未消除该限制，未获得真实模型响应；没有改动 Codex 认证、配置或权限。
  用户指定的 SimpleCADAPI 虚拟环境为 Python 3.10，仍有 DNS/socket 限制且缺少后端依赖；本批继续使用已有 Python 3.12。
- 2026-09-07 复测时目录与配置的宿主权限位均为 `777`，但当前受管会话内 `test -w` 仍为只读，
  loopback socket 返回 `EPERM`，Codex CLI 仍报告只读文件系统。说明宿主 chmod 已完成而会话级挂载/网络策略未重新加载；
  需要新会话后重新执行真实调用，不能把本次结果记为在线联调通过。

## 组件热接触批次

- 增加组件间热接触条件：源/目标组件、两侧稳定表面区域、单位面积接触热阻、可选有效面积和最大间隙，
  并纳入草案保存、用户确认、研究复制、Agent 上下文与优化候选参数。
- `SimulationSpec` 1.4 以带单位字段保存接触条件；无接触的旧版计划与规格继续忽略空字段，保持历史指纹兼容。
- 外露体素面使用相互最近邻、相反法向及间隙方向一致性配对；映射不到可靠近邻时阻止网格，不建立远距离隐式连接。
  单个面不能被多个接触重复占用，结果分别记录映射面积、有效面积、总导热系数和最大配对间隙。
- 每对接触面形成对称有限体积矩阵连接，参与稳态温度基准连通性检查；接触面不再同时施加环境换热。
  接触传热量单独进入结果收支，热流矢量包含跨组件接触传热，并保留能量守恒证据。
- 工况面板新增组件/区域/热阻/面积/间隙编辑器；组件树区域状态与三维画布以 `C` 工程标记显示两侧接触面。
  尚未完成真实浏览器布局验收、贴体界面网格、机械接触、压力依赖热阻和非线性接触状态。
- 验证：新增 `tests/test_contact_thermal.py` 3 项，覆盖类型/归属校验、两组件真实求解与能量守恒、远距离误连阻止；
  完整回归 126 项通过，耗时 57.41 秒。Ruff、JavaScript 语法、Node 草案状态和 Three.js 几何检查通过。

## 连续建模对话与草案同步批次

- 增加结构化草案保存、连续建模对话和建议应用/放弃/撤销接口；所有操作校验草案版本。
  草案保存重建带单位的 `SimulationSpec`，但不确认输入、不生成网格、不求解。
- GPT 读取当前已保存表单、紧凑几何/组件/区域元数据和最近对话；沿用严格结构化解析，不更换部署模型。
  服务端再次检查 Schema、材料记录来源、物理能力及引用；新物性数值需先由用户在材料表单中输入。
- 对话响应保存为独立待应用建议，使用可读名称和单位展示前后差异；显式应用后字段高亮待确认，支持单步撤销。
  已确认研究必须复制后修改；待处理建议会阻止最终确认。建议来源目前按参数组保存，不是每个嵌套数值。
- 请求生成建议期间允许表单继续编辑；迟到响应通过版本及几何、组件、区域指纹检查，不能覆盖新输入。
  同研究重复模型请求被拒绝；这是并发控制，尚不是全实例请求限流。
- 右侧增加可折叠建模对话抽屉，与画布和属性面板共存。表单自动保存串行执行，切换研究或发送对话前等待保存。
  保存冲突不会丢弃本地输入，重新载入放弃未保存修改前需确认；刷新前提示尚未保存的输入。
- 修复材料面板重绘丢失编辑、自动保存改变材料来源、覆盖其他判据及热源编辑与工况面板不同步的问题。
  缺少瞬态初态/时长/步长可保留为空继续追问，但不能通过求解确认。
- 新增 `THERMOFLOW_RETAIN_MODELING_HISTORY`，默认保留最近 40 条，模型最多读取最近 20 条。
  false 时新对话仅保留在当前页面内存，草案/建议/确认仍保存；不会主动清除其他历史研究或备份。
- 验证：新增 `tests/test_modeling.py` 13 项；全套 104 项通过，耗时 53.64 秒。
  Node 草案状态与 Three.js 几何检查、Ruff、JavaScript 语法检查通过。
- 尚未验收：真实 GPT 联网、真实浏览器渲染与布局、逐数值来源/选择性应用、完整自然语言能力评测、
  全实例限流与对话保留到期清理。浏览器再次在本地 socket 创建阶段返回 `EPERM`，未取得截图。
  直接检查 18018 本地端口也被相同权限限制拒绝；本批次未启动可访问预览服务。

## 区域热流、对流与辐射批次

- 增加稳定区域上的表面热流、对流和环境辐射，最多 48 条；热流正值流入，负值抽热。
  区域对流替换对应外露面的全局对流，同一区域可叠加热流、对流和辐射；同类重叠和定温单元冲突被阻止。
- 局部热源和全局对流可禁用，定温列表可为空。稳态逐连通组件检查温度基准；瞬态支持绝热冷/热初态及无散热加热。
  没有输入功率或温度参考时，温升/功率指标及其研究比较差值显示不适用，不再输出虚假的零。
- `SimulationSpec` 1.3 保存带单位的区域条件和发射率来源；旧版指纹兼容新增空/default 字段。
  旧区域能力迁移保留原始定义，新增条件同样受用户确认、快照一致性和网格质量检查约束。
- 辐射采用灰体表面对大型等温环境模型，使用阻尼 Newton 与稀疏线性求解。
  每步检查非线性残差和绝对温度，并保存收敛证据；不是通过固定换热系数冒充辐射。
- 结果及每个时间步保存热流、对流、辐射、定温边界、体热源和蓄热功率；前端时间切换读取对应时刻的收支。
- 修正零净换热平衡时相对误差被放大的问题：保留带符号的瓦特残差，另存各方程项绝对值之和作为归一化参考功率。
  瞬态参考包含隐式蓄热项，切换时刻同时切换参考功率；初始帧没有积分残差或参考功率。
  该指标只反映代数收支，不代表网格或时间离散误差，仍需检查原始残差与收敛性。
- 结构化表单新增条件类型、区域和数值字段，画布标出真实绑定位置并提供物理条件符号及悬停名称。
  新区域模型使用外露体素面积，旧全局对流路径保留历史修正以复现旧研究。
- 尚未完成：表面间视角因子、自遮挡、光谱/参与介质辐射、时变载荷、贴体边界、温变材料与真实浏览器验收。
  当前仍使用单元中心温度与体素表面积近似，必须进行网格收敛检查；任意粗网格的体素尺度偏移不保证单调收敛。

## 稳定选面与区域定温批次

- 主画布新增三角面拾取、增删选择、高亮、清空和命名保存；隐藏组件后仍使用原始 STL 面索引。
- 选区 ID 来自原始几何指纹与排序面集合，相同选择幂等；保存不确认研究、不触发求解，
  不重载或丢弃尚未确认的工况表单。已保存区域作为不可变定义供多个研究引用。
- STL 定温编辑从固定两端扩展为 1–24 条平面、组件表面或手工选区条件，用户仍须确认。
  已确认研究的条件不可直接改写；复制研究、热源编辑和优化候选保留完整边界列表。
- 增加 `SimulationSpec` 1.2 区域快照；新增无关区域不使旧研究失效，引用区域被改动则阻止计算。
  原 1.0/1.1 指纹忽略新增空字段；几何区域保存、单位修改和组件重命名共享互斥。
- 在外露体素面中心执行精确最近三角形查询；等距边缘先匹配外向法线，再按稳定面索引选择。
  定温施加于所属体素单元中心，仍是近似模型，不是贴体有限元边界。
- 网格阶段检查空映射、交界温度冲突、全域定温和少量映射单元；前端显示区域名、温度与映射数量，
  近似警告需要用户检查。稳态与瞬态能量守恒统一计入所有定温单元与自由单元间的导热。
- 尚未完成：框选/刷选和几何区域编辑撤销、贴体网格、映射单元专用着色、
  大规模映射性能及真实浏览器交互验收。本批次没有启用热机械或宣称完整稳定性评估。

## 三维工程画布批次

- 主画布已接入本地 Three.js r185 与 OrbitControls，许可证随包提供，不使用外部 CDN。
- 完整 STL 三角形接口保留稳定组件 ID 和用户确认尺度，不再将最多 1200 个抽样面作为主几何。
- 后端读取已完成且指纹匹配的 VTK，提取真实外表面；温度和热流数值直接取自节点/单元场。
- 通过 meshio 5.3 的独立适配层支持已有 VTK_VOXEL 文件，只转换连接顺序，不改写旧结果。
- 增加真实单元截面、完整网格差值、全场极值位置和单元中心探针；探针不依赖显示抽样。
- 接入标准视角、旋转/平移/缩放、透明度、组件显示筛选、定温边界选择、热源拖动、
  热流矢量、温度等值带、时刻切换和热点截面聚焦。时间播放等待对应场读取后再继续。
- 数据切换时清除旧场，不让上一研究或上一时刻的颜色冒充当前结果；WebGL 失败明确提示。
- 尚未完成：真实浏览器验收、大模型 LOD、固定探针、任意裁剪/等值面、分解视图、
  多画布并排比较、风险区域与全部 Agent 结论定位。旧二维绘制辅助函数尚待清理，主入口已停用。

## 后台计算批次

- 新增持久化、有界队列与独立数值子进程，普通网格/求解操作通过后台任务提交。
- 前端显示阶段、进度、耗时和取消入口；刷新后恢复服务端任务记录，不保存本地数值副本。
- 取消和超时终止实际计算进程；Linux 下服务端意外退出会终止所属计算子进程。
- 重启后排队任务继续执行，运行中断任务释放研究占用并保留确认快照，允许重新提交。
- “应用并求解”复制出独立研究并保留瞬态/材料设置；新网格有风险时暂停等待检查，
  不继承旧研究的网格风险确认。提交失败不会再被成功提示覆盖。
- 输入确认、网格风险确认和计算共享研究锁，阻止任务运行期间并发修改输入。
- 网格/结果发布与取消串行化；未完整发布的文件不能当作可用结果读取或下载，
  成功任务必须有有效产物指纹。排队阶段取消网格任务不会破坏已有网格和确认记录。
- 增加单数据目录调度器互斥、启动失败释放锁、未完成提交恢复、队列限制、
  独立任务超时上限和资源不足分类；诊断只记录短编号、异常类型及脱敏代码位置。
- 部署参数已补充到环境模板、README、架构和部署说明。
- 尚未覆盖：旧同步接口和 Agent 优化的后台化、操作系统硬内存配额、远程执行、
  数值迭代检查点恢复和大规模长期任务历史性能；不能将本批次视为全部稳定性需求完成。

## 热求解与数据契约

- 新增瞬态导热输入与服务器校验：初始温度、持续时间、时间步长、分析类型一致性和资源上限。
- 新增后向欧拉求解，按组件密度与比热计算热容；末步准确落在指定时长，能量平衡计入蓄热。
- 各时间步保存完整温度/热流 VTK 与压缩数值场、显示采样、真实极值位置、残差和守恒信息。
- 新增校验结果文件指纹的时刻读取接口；前端增加时间轴、播放暂停和整个时段固定温标。
- 温度判据和材料适用范围覆盖整个计算时段，避免冷却末态合格掩盖初始超温。
- 所需物理效应未实现时，即使简化模型判据满足，也显示无法判定完整目标。
- GPT 提示要求有来源材料、待确认草案、关键缺失问题和能力限制；上下文去除几何显示网格。
- 保留结构化 Responses 调用和服务端密钥，增加请求超时与有限重试。尚未进行真实 GPT 联网测试。
- 新规格支持 1.1，保持旧稳态 1.0 指纹兼容；拒绝非有限物理数值。
- 删除当前结果渲染中的装饰性热源光晕；最大热流密度直接来自已求解热流场。

## 验证证据

- `tests/test_transient.py`：解析冷却时间收敛、逐步能量守恒、末步时刻、确认阻断、单位契约、
  整个时段判据、完整场文件、指纹校验、旧版稳态指纹兼容。
- `tests/test_tasks.py`：真实后台网格/瞬态求解、进程取消与超时、队列和重复请求、
  重启恢复运行/排队任务、API 快速返回、半发布结果/网格隔离、网格风险暂停、
  确认锁、保留旧网格、未完成提交、重复调度器、资源诊断脱敏和环境参数范围。
- `tests/test_visualization.py`：完整 STL、稳定组件与尺度、VTK 节点/单元值、解析参考截面、
  内部单元探针、真实热流、瞬态时刻、全网格差值、跨组件映射与损坏文件拒绝。
- `tests/test_regions.py`：区域持久化与幂等、严格索引校验、三种单位的映射一致性、
  真实稳态/瞬态场与守恒、引用快照防篡改、空映射/温度冲突阻断、复制与优化边界保留、
  组件表面映射、原始 STL 指纹核验、旧区域能力迁移和几何写入互斥，共 12 项。
- `tests/test_surface_thermal.py`：Stefan–Boltzmann 辐射/对流解析平衡、瞬态辐射独立标量求根对照、
  对齐嵌套网格的一维导热收敛、绝热能量与热容、各连通体的稳态基准、区域覆盖冲突、
  全局对流替换、零净换热误差归一化、无热阻研究比较、Schema 单位与确认、发射率来源和未支持辐射范围，
  以及 Agent 紧凑区域上下文和候选参数保留，共 13 项。
- `node tests/browser/viewport_geometry.mjs`：Three.js 实际 BufferGeometry、数值色标、
  组件隐藏/隔离及 Raycaster 命中真实单元索引通过；此项不是浏览器渲染验收。
- 前一批区域热边界的项目、材料、确认、网格、求解、比较和 Agent 回归：91 项通过，耗时 53.47 秒，
  包含最新误差归一化修正；区域/表面边界/瞬态专项 29 项通过。Ruff 与 JavaScript 语法检查通过。
- 时间步参考功率同步的 Node 检查通过：读取选中帧、缺失值不继承末帧数据、不混用其他研究。
- 连续建模批次补充 13 项服务端测试，该批次全套为 104 项通过；`tests/browser/modeling_state.mjs`
  验证空缺输入、原判据/材料来源保留、保存串行化、迟到响应和页面内对话历史。两者均不能代替浏览器验收。
- `tests/browser/transient.mjs` 与 `seed_transient.py`：已编写真实计算驱动的桌面/手机截图、
  WebGL 像素、标准视角、缩放、时间播放、后台应用/求解、刷新恢复及提交失败提示检查。
  先前尝试中 Chromium 启动被系统调用限制拒绝；本批次再次执行时，Node 的本地端口
  监听即返回 `EPERM`，未进入浏览器检查。未取得截图，视觉和浏览器任务流程验收未通过。
  本批次改用 Python 子进程分配本地端口，仍在 socket 创建时遭到 `EPERM`；没有宣称取得截图。
  区域批次已补入真实选面、命名保存、保留未确认表单值及确认区域边界的浏览器检查；
  本次复测仍在创建本地 socket 时被拒绝，尚未执行页面步骤或生成截图。
  热流/辐射批次已补入区域条件确认与真实求解检查，但最新复测仍在本地 socket 创建阶段返回 `EPERM`。
- 当前执行环境无法访问外部依赖源和官方 API 文档。测试复用 `../env_cad` 数值依赖及本机缓存。
  沙箱内跨线程 asyncio 唤醒受限，临时测试运行器 `/tmp/thermoflow_pytest_runner.py` 加入周期唤醒；
  没有更改生产事件循环。普通部署应直接运行 `pytest`。
- 服务可以在工具进程内启动，但该沙箱的网络命名空间不能从另一工具进程访问，尚无已验证的用户可访问预览地址。
  区域批次使用隔离临时数据目录在 18016 端口完成 6 秒启动检查；热流/辐射批次在 18017 端口再次完成限时启动检查。
  服务正常启动并关闭，未遗留测试服务；这不是用户可访问性验证。

## 首期验收对照

| 条目 | 当前状态与剩余验证 |
| --- | --- |
| 1 STL、单位、真实模型 | 已接完整几何接口及 Three.js；真实浏览器尺度、旋转、缩放待完整验收 |
| 2 组件与区域调整 | 断开壳体、稳定 ID、重命名、隐藏、手工三角面选区已有；合并拆分、区域编辑撤销与浏览器验收尚缺 |
| 3 逐组件材料 | 各向同性常数热物性已有；完整材料编辑、来源授权、温变属性及界面参数尚缺 |
| 4 GPT 严格草案与追问 | 严格结构化调用、当前草案上下文和连续对话接口已有；真实联网、自然语言能力评测与浏览器闭环仍缺 |
| 5 用户检查确认 | 版本检查、建议差异、应用/撤销和独立确认已有；逐嵌套数值来源、选择性应用及浏览器体验仍需加强 |
| 6 边界空间绑定 | 平面/组件表面/手工选区定温、区域热流/对流/环境辐射及分离组件热接触已有；时变载荷、表面间辐射、机械接触及贴体边界尚缺 |
| 7 真实网格预览与质量 | 体素网格及基本质量已有；局部加密、体网格检查、特征尺度与自交风险检查尚缺 |
| 8 稳态和瞬态可复现研究 | 瞬态保存和普通后台任务已有；取消、超时、排队与重启恢复已测；更多 PDE 基准、网格收敛与生产资源验证仍缺 |
| 9 真实温度与热流、探针、极值、截面 | 已接 Three.js、完整 VTK 表面/截面/单元探针与极值；固定探针、一般裁剪/等值面、大模型 LOD 和浏览器验收尚缺 |
| 10 热机械模型与真实场 | 尚未启用或实现，应力、形变与模型验证待后续实施 |
| 11 状态与目标区分 | 已修复未支持效应仍返回满足目标的问题；全部文案与状态入口仍需审计 |
| 12 判据不足无法判定 | 后端覆盖无判据、材料范围不足、未支持物理；持续时间与失效模式的完整工程判断仍需扩展 |
| 13 Agent 引用与查看结果联动 | 查看研究结果已有；全部 Agent 引用绑定物理量、区域、时间与空间焦点尚缺 |
| 14 普通界面无内部输出 | 已有隐藏和摘要处理；所有错误路径、导出文件显示名和遗留界面需完整审计 |
| 15 脱敏高级诊断 | 已有单独入口；脱敏诊断包、分级权限和服务端结构化审计尚缺 |

## 后续实施范围

1. 完善温变材料、压力/温度依赖热接触、时变载荷、辐射模型范围和物理能力接口；加强贴体边界及独立数值验证。
2. 完善后台任务浏览器验收、优化任务接入、硬资源隔离和长期任务历史性能；保持不可变研究快照。
3. 完成连续建模对话真实联网和浏览器验收，扩展逐字段来源、选择性应用与多步撤销；强化能力评测、全实例限流和保留到期策略。
4. 完成三维画布浏览器验收、固定探针/一般截面与比较时刻、LOD、风险区域联动、结果导出和工程报告。
5. 完善几何修复预览与撤销、组件合并拆分、区域刷选/框选与编辑撤销、复杂装配体能力边界。
6. 实施线性热机械及其数值验证，并逐步扩展文档规划的高级物理；未实现部分继续明确标注。
7. 完善项目权限隔离、上传资源限制、诊断脱敏与优化授权预算；按全部需求逐项验证后才能宣布完成。
