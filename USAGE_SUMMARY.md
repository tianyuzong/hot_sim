# ThermoFlow 使用说明与当前状态

更新日期：2026-09-08  
工程目录：`/data/yihongzhu/_zty/hot_sim`

## 1. 当前结论

- ThermoFlow 服务当前可以运行，工作台地址是 `http://127.0.0.1:18080/docs`。
- **Codex 目前不是“不可用”状态。** 已确认服务器上的 Codex CLI 可以执行、已有登录状态，ThermoFlow 最近完成的算例也记录了 `codex-cli` 规划来源。
- 当前 Codex CLI 版本为 `0.153.4`，最近算例实际记录的模型为 `gpt-5.5`。
- Codex 负责把自然语言需求整理成结构化仿真方案，网格和温度场由 ThermoFlow 的确定性数值求解器计算，不是由 Codex 直接生成。
- 当前 Firework 部署已启用 `CuPy + CUDA`，固定使用空闲的 NVIDIA RTX 4090；健康检查会显示 `cuda-cupy`，GPU 实算结果会显示 `cuda-cupy-cg` 和设备名称。
- 瞬态求解完成后，页面会一次加载共享网格和全部真实温度帧；加载完成后拖动时间轴、连续播放都从浏览器本地缓存绘制，不再逐帧请求服务器。
- 瞬态结果默认打开“热扩散”视图，用固定的全时段空间温差色标显示哪里先热、热量怎样向外传播；也可切换“绝对温度”，两种模式的色标数字在播放时都保持不变。
- `README.md` 中“Codex 不可用、完整流程尚未验证”的旧说明已经过时，应以本文件和实时检查结果为准。
- 统一 Agent 会话支持 ThermoFlow 文档问答、当前研究结果解释，以及从自然语言补齐草案并提交后台仿真；问答引用和会话记录由服务端保存。

## 2. 如何启动

先登录 Firework 服务器，然后执行：

```bash
cd /data/yihongzhu/_zty/hot_sim
./.venv312/bin/thermoflow
```

服务以前台方式运行时，不要关闭这个终端。停止服务可以在该终端按 `Ctrl+C`。

启动后可检查健康状态：

```bash
curl http://127.0.0.1:18080/health
```

返回内容中出现 `"status":"ok"` 表示服务正常。当前部署应同时显示：

- `planner_mode: codex`
- `agent_mode: codex-cli`
- `selected_backend: cuda-cupy`

不要在同一个数据目录重复启动多个 ThermoFlow 服务进程。

## 3. 如何打开界面

在已经建立 Firework SSH 端口转发的本机浏览器中打开：

- 主工作台：`http://127.0.0.1:18080/docs`
- 开发者 API 文档：`http://127.0.0.1:18080/api/docs`

如果页面打不开，依次检查：

1. Firework 服务器上的 ThermoFlow 进程是否还在运行。
2. `curl http://127.0.0.1:18080/health` 是否成功。
3. 本机到服务器的 `18080` 端口转发是否仍然连接。
4. 浏览器强制刷新一次页面。

## 4. 完整操作流程

### 4.0 使用统一 Agent

工作台的“Agent 问答与建模”区可以直接提问，例如“瞬态导热需要哪些参数？”，答案会显示 ThermoFlow 文档来源。也可以描述仿真目标；Agent 会在材料、瞬态时间和边界缺失时展示选项或输入框，并把回答写入未确认草案。摘要检查和材料确认完成后，点击“确认摘要并开始仿真”提交后台任务。Agent 不会绕过 STL 尺度确认、策略校验或网格风险复核。

问答默认读取当前研究的参数和结果摘要，但只读回答不会修改研究。文档没有覆盖的内容在启用模型服务时会标记为通用知识；离线模式只回答本地文档已覆盖的内容。

### 4.1 选择工件

1. 打开主工作台。
2. 在左侧选择项目、几何版本或已有算例。
3. 如果导入新的 STL，必须确认单位。STL 本身不保存单位，单位选错会使尺寸、网格和热源位置全部出错。
4. 在三维画布中旋转、平移和缩放，确认工件方向及尺寸。

### 4.2 设置材料

在材料面板选择或填写：

- 导热系数；
- 密度；
- 比热容。

做瞬态仿真时，密度和比热容都会参与计算，不能只填写导热系数。

逐组件核对完成后，必须勾选“我已逐组件确认当前材料、导热系数、密度和比热容”。未完成这项独立确认时，“确认仿真输入”按钮不可用；直接调用确认接口也会被拒绝。材料被再次修改或采用了新的 Agent 建议后，需要重新勾选确认。

### 4.3 设置仿真场景

1. 选择稳态或瞬态分析。
2. 设置初始温度、环境温度、仿真时长和最大积分步长。界面中的“时间步长”是求解器内部步长上限，不再代表保存帧必须等间隔。
3. 根据需要设置固定温度、对流、热流或辐射边界。
4. 点击画布旁的“编辑热源”，用“新增热源”创建热源。每个热源行右侧都有独立的 `×` 删除按钮，底部“删除当前：名称”也会明确显示将删除谁；一个研究最多可设置 16 个独立热源。
5. 每个热源可以单独命名、设置功率、位置并选择类型：
   - 点热源；
   - 线热源；
   - 面热源；
   - 体热源（用中心坐标及 X/Y/Z 三向尺寸定义长方体区域）。
6. 点、线、面、体热源可以混合使用，求解器会叠加各热源功率。

瞬态算例的 `t = 0` 是确认的初始温度场。例如初始温度为 `293.15 K` 时，开始帧应当全部是冷态；热源附近会从后续时间帧开始升温。
新建或从短时算例打开热源编辑器时，推荐的演示时长为 `60 s`；最大积分步长可设为 `1 s`。新求解会按实际网格和材料热扩散率保存最多 201 个真实帧：早期更密、后期更疏，并精确覆盖到 `60 s`；每帧都来自数值积分，不是插值动画。

### 4.4 生成网格并求解

1. 确认方案。
2. 生成网格。
3. 检查单元数量、边界映射数量和网格质量。
4. 如果出现非阻断警告，阅读警告后手动接受，再提交求解。
5. 等待任务完成。刷新页面不会删除已经持久化的任务和结果。

当前求解器使用体素离散，因此表面边界和热源会映射到最近的有效单元。较小热源、尖角或很薄的工件需要使用不同网格尺寸做收敛性检查。

### 4.5 查看结果

算例完成后，在结果区可以：

- 查看默认的“热扩散”空间温差场，或切换“绝对温度”场；
- 查看热流场；
- 播放瞬态时间序列；
- 打开瞬态结果时，先等待“一次加载 N 个真实温度帧…”提示消失；
- 任意拖动时间滑块到已经求解的时刻；加载完成后会立即从本地缓存显示目标时刻，不产生逐帧网络请求；
- 切换截面和温度色带；
- 点击工件读取局部温度、热流和单元信息；
- 查看最高温度、最低温度及热点位置。

“热扩散”把每个点显示为 `T(x,t) − Tmin(t)`，色标固定为 `0` 到整个时段最大的单帧空间温差，适合观察热源附近先热、远处后热；图例下方同时显示当前帧绝对参考温度 `Tmin(t)`。“绝对温度”使用整个时段统一的绝对温标。播放过程中两种模式的低、中、高三个数值都不会跳动，变化的是工件颜色。

温度场按真实求解单元着色，网格较粗时会看到分块；这不是前端随机上色。需要更平滑且更可信的结果时，应减小网格尺寸后重新计算，而不是只做视觉插值。若整个时段的空间温差低于数值容差，页面会明确显示“近似等温”，不会放大计算噪声。

如果整个时段的最低/最高温度超出所选材料的有效温区，结果页会显示材料、组件、有效温区和结果温区告警。这表示当前常物性模型正在外推，且没有建模温度相关物性和相变，结果不能直接作为工程结论。

温度场时间序列会一次预载；热流场、截面和结果差值仍按需读取，因为这些视图的数据量和选择参数不同。

## 5. 如何移动热源并观察变化

推荐从已经完成的瞬态算例开始：

1. 在左侧选择已完成的算例，例如“移动热源瞬态演示”。
2. 打开结果中的“热扩散”视图；需要核对绝对温度时切换到“绝对温度”。
3. 点击“编辑热源”。
4. 在列表中点击名称，或者直接单击画布上的热源标记来指定要操作的热源；当前热源为红色，其余热源为橙色。
5. 选择点、线、面或体热源。在画布上拖动对应标记；也可以直接修改位置坐标。线热源的起点、终点都可拖动，面和体热源可拖动中心。
6. 拖动时，画布显示的是快速近似预览，用来直观看热区随位置变化。
7. 位置确定后点击“应用并求解”。系统会新建一个算例，重新划分/检查网格并完成正式求解。
8. 在新算例结果中播放时间序列，查看热源位置附近从冷态逐步升温的过程。

如需删除某一个热源，直接点击该热源名称右侧的 `×`；其他热源会原样保留。至少需要保留一个热源，如需完全禁用热源，请在工况页关闭“启用局部热源”。

修改热源不会覆盖旧算例，旧结果会保留，便于前后对比。

需要特别说明：当前版本支持“拖到新位置后重新求解”，但热源位置在一次瞬态计算期间保持不变。也就是说，现阶段还没有实现热源沿轨迹连续运动的移动热源模型。

## 6. 关于画布和热源显示

- 工件左右两侧原先重复出现的两个小温度标签已经移除。
- 热源位于哪里，后续时间帧的主要升温区域就应当从哪里开始。
- 如果热源放在几何中心但热点明显偏到一侧，先确认正在查看的是否为新算例、时间是否大于零，以及热源位置是否已经“应用并求解”。
- 由于热源会映射到最近的体素单元，显示位置与连续坐标之间可能有半个网格左右的偏差。
- 旧结果不会随着未提交的拖动被改写；拖动过程是预览，正式结果必须重新求解。

## 7. Codex 的当前状态

目前 Codex 集成是可用的，不需要把系统切回离线规划器。

已确认的状态包括：

- ThermoFlow 健康检查显示 `planner_mode = codex`；
- Codex CLI 可执行，版本为 `0.153.4`；
- Codex 已有有效登录状态；
- 最近成功算例的规划记录为 `provider = codex-cli`、`model = gpt-5.5`；
- Codex 调用失败时不会悄悄退回离线方案。

需要区分两件事：

1. **Codex 规划可用**：可以根据自然语言生成或调整结构化仿真设置。
2. **数值仿真可用**：优先由 Firework 上的 NVIDIA RTX 4090 和 CuPy/CUDA 求解器生成真实温度场和热流场；健康检查和结果详情会显示实际计算后端。

因此，Codex 是否可用不决定已有算例能否查看，但会影响自然语言建模和 Agent 调参功能。

## 8. 当前限制

- Firework 部署已启用 `cuda-cupy`；如果 CUDA 环境异常，强制 GPU 模式会明确报错，不会静默冒充 GPU 结果。
- 当前后端是体素热传导模型，不是通用有限元软件。
- 一次瞬态求解中的热源和边界条件保持恒定。
- 旧瞬态结果仍可播放，但只有重新求解的新结果才具有按网格和材料扩散率生成的早期密集真实帧。
- 目前不支持热源沿自定义轨迹连续运动；只能拖到新位置后创建新算例并重新求解。
- 网格越细，结果通常越平滑，但计算量和内存占用也越高。
- 工程判断应结合网格收敛性、时间步长收敛性、材料适用温度范围和能量平衡检查。

## 9. 最短演示步骤

只想快速演示时，可以按下面操作：

1. 打开 `http://127.0.0.1:18080/docs`。
2. 选择“移动热源瞬态演示”。
3. 点击“编辑热源”，连续点击“新增热源”，建立点、线、面、体四个热源并分别设置位置和功率。
4. 把仿真时长设为 `60 s`、时间步长设为 `1 s`，点击“应用并求解”。
5. 若出现网格质量提示，检查后勾选接受，再点击“开始求解”。
6. 打开“热扩散”，把时间滑块放在 `t = 0`，确认工件是初始冷态；随后观察热源附近先出现高温色并向外扩散。
7. 点击播放，观察四个热源附近逐步升温。
8. 再次打开“编辑热源”，在列表中选中任一热源并拖动；重新求解后确认热点随该热源位置变化。
